{
  "name": "css-grid",
  "version": "1.0.0",
  "description": "Learn the fundamentals of CSS Grid and build real world layouts",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server --files '**/*.html, **/*.css, **/*.js, !node_modules/**/*' --directory"
  },
  "author": "Tyler Bramer",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.24.4"
  }
}
